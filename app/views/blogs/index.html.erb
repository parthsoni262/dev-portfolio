<div>
  <h3><%= link_to 'Write New Blog', new_blog_path if logged_in?(:site_admin) %></h3>
</div>

<div class="col-md-8">
  
  <hr>

  <%= paginate @blogs %>

  <br>
  
  <%= render @blogs %>  
  
  <%= paginate @blogs %>
</div>