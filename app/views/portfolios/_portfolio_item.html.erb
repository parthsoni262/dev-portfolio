<div class="col">
  <div class="card shadow-sm" data-id="<%= portfolio_item.id %>">
    <img src="https://place-hold.it/350x200">
    <div class="card-body">
      <p class="card-text">
        <span><%= link_to portfolio_item.title, portfolio_show_path(portfolio_item) %></span> | <%= portfolio_item.subtitle %>
      </p>
    </div>
  </div>
  <br>
</div>